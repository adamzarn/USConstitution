# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target ‘Constitution Quizzer’ do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Constitution Quizzer

    pod 'Firebase/Core'
    pod 'Firebase/Database'
    pod 'Firebase/Auth'

  target ‘Constitution QuizzerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target ‘Constitution QuizzerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
